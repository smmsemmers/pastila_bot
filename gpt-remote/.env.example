# pastila-gpt-remote (@pastila_gPT_remote_bot) — environment template
# Copy to .env and fill in real values. Never commit .env.

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# LLM: если задан OPENROUTER_API_KEY — бот идёт через OpenRouter с авто-подбором
# лучшей модели под задачу (chat→Claude Sonnet 5, code→GPT-5.2 Codex,
# reasoning→o3, vision/OCR→Gemini 2.5 Pro). Иначе откат на прямой OpenAI.
OPENROUTER_API_KEY=

# OpenAI (нужен только для отката и/или Codex CLI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Access control (comma-separated Telegram numeric IDs)
ALLOWED_USER_IDS=
ALLOWED_CHAT_IDS=

# Codex / local command execution (optional)
ENABLE_CODEX=false
CODEX_WORKDIR=
CODEX_SANDBOX=
CODEX_TIMEOUT_MS=
